server {
        listen 8080;

        root /usr/share/nginx/html;

        location /flowcs {
            index index.html;
            try_files $uri $uri/ $uri.html /flowcs/index.html;
        }
}

